spring:
  application:
    name: recommendation-service

server:
  port: 7002
  error:
    include-message: always

logging:
  level:
    root: INFO
    thehappycode: DEBUG
---
spring:
  application:
    name: recommendation-service
  profiles:
    active: docker

server:
  port: 8080
