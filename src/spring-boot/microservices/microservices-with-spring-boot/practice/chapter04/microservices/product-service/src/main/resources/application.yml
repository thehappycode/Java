spring:
  application:
    name: product-service

server:
  port: 7001
  error:
    include-message: always

logging:
  level:
    root: INFO
    thehappycode: DEBUG
---
spring:
  application:
    name: product-service
  profiles:
    active: docker

server:
  port: 8080
