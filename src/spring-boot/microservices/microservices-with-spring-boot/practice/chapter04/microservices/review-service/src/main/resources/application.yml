spring:
  application:
    name: review-service

server:
  port: 7003
  error:
    include-message: always

logging:
  level:
    root: INFO
    thehappycode: DEBUG
---
spring:
  application:
    name: review-service
  profiles:
    active: docker

server:
  port: 8080
